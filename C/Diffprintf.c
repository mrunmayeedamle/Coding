/*Subtraction of 2 numbers CSBS-07*/
#include <stdio.h>
int main ()
{
    int a, b, diff;
    a = 30, b = 90;
    diff = (a - b);
    printf("The difference between a and b is :%d", diff);
    return 0;
}
