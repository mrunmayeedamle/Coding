/*Subtraction of 2 numbers CSBS-07*/
#include <stdio.h>
int main ()
{
    int x, y, Diff;
    printf("PLease enter x and y :\n");
    scanf("%d%d", &x, &y);
    Diff = (x - y);
    printf("The difference between x and y is :%d", Diff);
    return 0;
}
