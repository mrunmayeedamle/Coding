/* Multiplication of 2 numbers CSBS-07*/
#include <stdio.h>
int main()
{
    int m, n, Prod;
    printf("Enter the values of m and n:\n");
    scanf("%d%d", &m,&n);
    Prod = (m * n);
    printf("The product of m and n is :%d", Prod);
    return 0;
}
